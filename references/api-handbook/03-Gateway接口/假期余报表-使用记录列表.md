# 假期余报表 · 使用记录列表（二方接口）

> 接口编号：#115（03-Gateway接口 扩展）  
> 端点：`GET /web/gateway/attendance/api/record/list/...`  
> Host：`v5.ihr360.com`  
> 鉴权：前端插件 Cookie+XSRF｜宿主 AI 环境：`Authorization: Bearer ${IHR360_API_TOKEN}`（环境变量自动注入，Base URL `v5.ihr360.com`）

---

## 描述

「假期余报表」按员工+假期类型查**使用记录列表**（可能是休假单的详细信息/扣减流水）。与 #114 额度单列表配合使用：额度单是发放/调整，使用记录是实际使用。

> ⚠️ 2026-08-04 抓包确认端点路径族（GET），**完整 URL 与响应待实测**。

---

## 请求参数

| 参数 | 位置 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| leave_setting_id | query | String | 是 | 假期设置 ID |
| r_id | query | String | 是 | 租户/路由标识 |

（具体参数待实测补全）

---

## 响应数据

⏳ 待实测

---

## 关联页面

- 路由：`#/timeManage/holiday-surplus`
- 页面标题："XX 的假期使用记录"

---

## 对问题2 的价值

- 与 #114 配合：114 看额度单（发放/调整），115 看使用记录（扣减）
- 完整链路：发放（#114）+ 使用（#115）+ 余额（#80/#82）→ 还原计算链
