# 假期余报表 · 额度单列表（二方接口）

> 接口编号：#114（03-Gateway接口 扩展）  
> 端点：`GET /web/gateway/attendance/api/limitBill/list/...`  
> Host：`v5.ihr360.com`  
> 鉴权：前端插件 Cookie+XSRF｜宿主 AI 环境：`Authorization: Bearer ${IHR360_API_TOKEN}`（环境变量自动注入，Base URL `v5.ihr360.com`）

---

## 描述

「假期余报表」按员工+假期类型查**额度单列表**（发放/调整记录）。是页面表格数据的来源。

> ⚠️ 2026-08-04 抓包确认端点路径族（GET），**完整 URL 与响应待实测**。

---

## 请求参数

| 参数 | 位置 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| id | query | String | 是 | 假期类型 ID |
| leave_setting_id | query | String | 是 | 假期设置 ID |
| r_id | query | String | 是 | 租户/路由标识 |

（具体参数名/路径待实测补全）

---

## 响应数据

⏳ 待实测

---

## 关联页面

- 路由：`#/timeManage/holiday-surplus`（时间管理 → 假期余报表）
- 数据展示：员工"假期使用记录"——按假期类型标签下的明细记录

---

## 对问题2 的价值

- **最接近额度数字的接口**：额度单列表 → 包含历次发放/调整/剩余，是补齐计算链（发放 → 已用 → 余额）的关键
- 配合 #113 `limitBill/leaveSettingType`（哪些类型有额度单）使用：先按类型查，再按员工查该类型下的额度单
