# 假期余报表 · 额度单假期类型（二方接口）

> 接口编号：#113（03-Gateway接口 扩展）  
> 端点：`GET /web/gateway/attendance/api/limitBill/leaveSettingType`  
> Host：`v5.ihr360.com`  
> 鉴权：前端插件 Cookie+XSRF｜宿主 AI 环境：`Authorization: Bearer ${IHR360_API_TOKEN}`（环境变量自动注入，Base URL `v5.ihr360.com`）

---

## 描述

「假期余报表」页面的额度单假期类型查询。返回**有额度单（发放/调整记录）的假期类型**，供报表按类型展示额度单数据。

> ⚠️ 2026-08-04 抓包确认端点与参数（GET 200 OK），**响应体待实测**（结构应与 #112 类似，返回假期类型列表）。

---

## 请求参数

| 参数 | 位置 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| r_id | query | String | 是 | 租户/路由标识（如 `8iljq7z3a3939s8e`） |
| u_id | query | String | 是 | 用户标识（如 `992311676526956544`） |

## 请求示例

```
GET /web/gateway/attendance/api/limitBill/leaveSettingType?r_id=8iljq7z3a3939s8e&u_id=992311676526956544
Host: v5.ihr360.com
```

---

## 响应数据

⏳ 待实测（预计返回假期类型列表，含额度单标记）

---

## 关联页面

- 路由：`#/timeManage/holiday-surplus?menu=108&subMenu=15007&thirdMenu=1500704`（时间管理 → 假期余报表）

---

## 对问题2 的价值

- **确认第三路径族**：`/web/gateway/attendance/api/limitBill/...` = 假期**额度单**（发放/调整记录）接口族
- 本接口是"哪些类型有额度单"的前置配置；**额度单明细（发放/调整/剩余）应由同族下的 search/query/list 类接口返回**，待继续抓包定位
- 三个二方路径族全景：
  - `/leaveSetting/...` 假期设置（#111）
  - `/limitutil/...` 限额工具（#112）
  - `/limitBill/...` 额度单（#113）
