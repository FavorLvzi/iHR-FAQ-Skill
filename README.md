# 进阶数字支持（ihr360-faq-thinking）v0.1

**i人事（iHR360）业务分析思考引导技能（Skill）**

面向「i人事」HR SaaS 平台的业务分析助手技能：当用户询问某个考勤/薪资/组织等字段或指标"怎么算、为什么是这个数"时，引导 AI 还原计算链、定位配置依赖、暴露口径歧义、给出查证路径，输出结构化的分析结论。

## 定位

- **思考引导型 Skill**：产出"分析思路与答案"，而非页面
- 覆盖口径溯源、异常诊断、趋势归因、操作指引四类问题
- 与「页面生成型」Skill（rowan-ihr360-openapi-skill）互补：前者讲道理，后者摆数据

## 目录结构

```
进阶数字支持/
├── SKILL.md                    # 入口（角色 + 分析框架 + 输出规范）
├── references/
│   ├── sources.md              # 内容锚点（官方语雀知识库 + query_faq 搜索接口）
│   ├── problem-index.md        # 场景化问题入口（按问题定位分析路径）
│   ├── output-rules.md         # 输出规范
│   ├── attendance-field-logic.md  # 考勤字段计算逻辑 + 节点递归规则 + 鱼骨图排查法
│   ├── api-reference.md        # 业务链路速查（按问题找接口链）
│   ├── formula-language.md     # iHR360 公式语言（70+ 函数 / 典型场景）
│   └── api-handbook/           # 接口手册（9 大模块，OpenAPI #1~#117 + 二方 Gateway）
└── examples/                   # 方法论示例（真实场景还原链路）
```

## 核心能力

1. **口径溯源**：字段/指标"怎么算"——区分内置字段（文档口径）与自定义字段（API 公式），还原计算链
2. **异常诊断**：考勤异常/操作被拦——数据溯源 + 鱼骨图排查法（按"用户自查→系统配置"逐类排查）
3. **流程走向**：具体员工发起某审批走哪条分支——员工定位 → 属性匹配 → 条件分支 → 输出 Mermaid 全流程图（命中分支高亮）
4. **假期余额**：余额快照 + 规则 + 额度变更流水还原完整计算链
5. **薪资公式**：按员工+方案+月份还原公式并验证数值

## 数据来源约定

- **接口规格**：本地 `api-handbook/`（99+ 接口文档，双向同步自 API 参考手册）
- **业务口径**：官方语雀知识库（`ihr360.yuque.com/ihr/help` 与 `/ihr/faq`）——优先走 `query_faq` 官方搜索接口（`IHR360_API_TOKEN` 环境变量鉴权，勿硬编码）
- **字段公式**：内置字段走文档；自定义字段公式+值走 API

## 使用前提

- 宿主 AI 环境需具备：联网抓取能力（WebFetch）+ `IHR360_API_TOKEN` 环境变量
- OAuth 客户端凭证（AppID/AppSecret）由客户开放平台提供，用于 OpenAPI 全量接口

## 版本与整改日志

### 版本

- **v0.1.0**（2026-08-15 定名「进阶数字支持」）——六大问题收口：日报字段溯源 / 假期余额 / 加班额度 / 薪资公式 / 流程走向 / 鱼骨图排查法

### 整改日志（完整修改流程）

> 记录 skill 从创建到当前的全部关键变更，便于追溯"为什么这样改"。

**2026-07 · 定位确立期**
- 思考引导型定位确立：与「页面生成型」skill（rowan-ihr360-openapi-skill）职责分离——前者讲道理（分析思路+答案），后者摆数据（HTML 看板）
- 内容锚点规则：口径事实只认官方语雀（`ihr360.yuque.com/ihr/help` 与 `/ihr/faq`），不引用官网博客
- 鉴权双轨体系：OAuth（客户 AppID/AppSecret，OpenAPI 全量）+ 宿主 `IHR360_API_TOKEN`（二方 Gateway）

**2026-08-07 ~ 08-11 · 六大问题收口期**
- **Q1 日报字段溯源**：内置字段走文档口径、自定义字段走 API 公式（#93 日报 + #96 字段定义关联查）
- **Q2 假期余额**：余额快照 + 规则配置 + #116 额度变更流水 → 还原完整计算链
- **Q3 加班额度**：加班单 + 结转（转调休/转薪资）+ 锁定账本 → 净锁定还原
- **Q4 薪资公式**：185 项字段公式明文（二方接口）+ 四步解读法（拿公式→拆解→讲清→值验证）
- **Q5 流程走向**：三层定位链路（findApprovalGroupVo → approval/setting 已发布版）+ hasUnPublishDraft 解锁草稿 + Mermaid 流程图输出规范（命中分支高亮）
- **Q6 鱼骨图排查法**：沉淀 `fishbone-method.md`——"功能用不了"类问题按"用户自查→系统配置"逐类排查

**2026-08-12 · 实测与精简**
- 实测 `findApprovalGroupVo` 可用（定位审批类型首选接口）
- 补卡流程正式版流程定义（含嵌套并行 Gateway + HandleTask 办理人）
- SKILL.md 精简 213→162 行；新增 `problem-index.md`（场景化问题入口）+ `output-rules.md`（输出规范）

**2026-08-13 · 结构与能力增强**
- 结构优化：05-薪资 20→11、07-考勤 27→13 子领域合并；04-枚举归位；两套 API 参考划界（业务链路速查 vs 接口目录索引）
- 整合 `query_faq` 官方搜索接口：按问题直接搜官方知识库（返回语雀链接+内容），解决"仅知根网址无法导航到具体文档"问题
- 补强节点递归规则（ParallelGateway 必须递归 conditionNodes→ParallelFlow.childNode，避免漏并行分支办理人）
- 补强日报自定义字段 #93+#96 关联查询指引
- 修复宿主读取目录（iHR-F&Q）为空的问题

**2026-08-14 · 接口收录**
- 收录「按手机号获取员工 ID」接口（getStaffIdByMobileNo）——员工定位新增手机号直查途径

**2026-08-15 · 正式定名**
- 重命名：`ihr360-faq-thinking` → **「进阶数字支持」v0.1.0**；目录/zip/PDF/甘特图同步更名

**2026-08-18 · 权限模块（已回退）**
- 新增 `10-权限管理` 模块（用户授权场景：authcenter/authority/manage 域 13 个二方接口文档）——**2026-08-19 已删除，见下**
- 实测 dataAuth 读取限制：完整数据仅浏览器会话可用，HTTP 直调（Python/宿主代理）返回空模板

**2026-08-19 · 公开同步与回退（当前）**
- **删除 `10-权限管理` 模块**（13 个文档 + index/README 引用）——内部二方接口文档不随公开仓库发布
- **PII 脱敏**：真实员工名 → 化名（员工A/B/C/D/E/F/G/X/I/J/K）、真实手机号 → 示例号，公开前全量扫描
- **同步至 GitHub 公开仓库**（FavorLvzi/iHR-FAQ-Skill，94 文件）；新增 README.md + .gitignore（排除 zip/凭证）
- PDF 说明同步删除「用户授权」章节（2 页 83KB）
